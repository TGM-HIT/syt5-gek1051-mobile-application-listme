# Watch Bluetooth Sync — only needed in dev (production uses window.location.origin automatically)
# Set this to your dev machine's local IP + backend port so the Pixel Watch can reach it over WiFi.
# Find your IP with: ip addr show | grep "inet " (Linux) or ipconfig (Windows)
# VITE_WATCH_API_URL=http://192.168.1.42:8080
